extends ../layout/main

block variables
  - barba = 'events'

block content
  section.section-events
    .container
      h4.title-h6.section-events__subtitle Our blog
      h1.title-h2.section-events__title Createx School Journal

      // .section-blog__filters
        .tabs
          a(href="#") All
          a(href="#") Articles
          a(href="#") Videos
          a(href="#") Podcasts

        select
          option all themes 
          option Marketing 
          option Development 
          option Design
          option HR & Recruting
        
        .section-blog__search
          input(type="text", placeholder="Search blog...")

      .section-events__event
        each item in [1, 2, 3, 4, 5, 6, 7, 8]
          +card-event(item)
      
      +pagination

  include ../section/subscribe